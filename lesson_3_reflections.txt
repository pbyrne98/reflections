When would you want to use a remote repository rather than keeping all your work local?

When mulitiple peole are contributing to the repository


Why might you want to always pull changes manually rather than having Git automatically stay up-to-date with your remote repository?

Files may not be ready for publication


Describe the differences between forks, clones, and branches. When would you use one instead of another?

Fork, creates a copy directly  on GitHub
Clone, creates a copy from GitHub or from your local repositories
Branch, label to copy the current state of commit's in a repository to another name within the repository


What is the benefit of having a copy of the last known state of the remote stored locally?

Allows multiple people and multiple devices to update to the GitHub repository


How would you collaborate without using Git or GitHub? What would be easier, and what would be harder?

Use initals, date and timestamp as a key, with a ongoing text file with changes made in each directory
Git, as it automates a lot of this - especially the logging of commits. 
The main function of Git is built on commits. Any alternative would require something as good.



When would you want to make changes in a separate branch rather than directly in master? What benefits does each approach have?

While working on a 'new' feature, this allows the master to say stable for others to start collaboration on.


